@model IEnumerable<SicoobTeste.Models.Users>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sobrenome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataDeCriacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleCodeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sobrenome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataDeCriacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleCodeNavigation.TipoDeUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UsersID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UsersID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UsersID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
